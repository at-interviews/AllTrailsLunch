/// An enumeration that represents the different display modes available in the UI.
///
/// `DisplayType` is used to indicate whether content should be displayed in a list format
/// or on a map. This is useful for toggling between different views or layouts within using
/// CTA button.
enum DisplayType {
    case list
    case map
}
